import {
    StyleSheet
} from 'react-native';
const IndexStyle = StyleSheet.create({
    listView: {
        borderColor: 'blue',
        borderStyle: 'dashed',
        borderWidth: 1,
        flex:1,
        justifyContent:'flex-start'
    }
});